# Mint Anime Blog Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and verify a deployable Chinese VuePress 2 blog for `dfghiyu/blog` with a mint anime-tech visual identity.

**Architecture:** Keep VuePress content in `docs/`, theme and site behavior in `docs/.vuepress/`, and deployment in `.github/workflows/deploy.yml`. Use `vuepress-theme-hope` for search, dark mode, taxonomy pages, and article chrome; use a small custom homepage and SCSS layer for the distinctive visual system.

**Tech Stack:** Node.js 22, pnpm, VuePress 2, vuepress-theme-hope, TypeScript, Markdown, SCSS, GitHub Actions Pages.

## Global Constraints

- VuePress base must be exactly `/blog/`.
- Site title must be `韩子阳的博客`; description must be `记录技术、学习与生活`.
- Visual direction is white/light-cyan, deep teal, mint, card-based, and dark-mode capable.
- Anime visual must be an original silver-gray-haired quiet guitarist placeholder, not a copy of a protected character.
- Deployment target is GitHub repository `dfghiyu/blog` and the `main` branch.
- Do not add secrets, tokens, or private data.

---

### Task 1: Create package and VuePress configuration

**Files:**
- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `docs/.vuepress/config.ts`
- Create: `docs/.vuepress/client.ts`

**Interfaces:**
- Produces `pnpm docs:dev`, `pnpm docs:build`, and `pnpm docs:clean` scripts.
- Produces a VuePress site with `base: '/blog/'`, Hope theme, taxonomy pages, search, dark mode, and GitHub repo link.

- [x] **Step 1: Add package metadata and scripts** with Node 22 engine and pinned compatible dependency ranges.
- [x] **Step 2: Add VuePress theme configuration** for Chinese locale, navbar, sidebar, edit links, footer, and taxonomy pages.
- [x] **Step 3: Add client-side registration** for the custom homepage component if needed.
- [ ] **Step 4: Validate configuration shape** by running `pnpm install` and a first build after content exists.

### Task 2: Create content and homepage

**Files:**
- Create: `docs/README.md`
- Create: `docs/posts/README.md`
- Create: `docs/posts/hello-vuepress.md`
- Create: `docs/category/README.md`
- Create: `docs/tag/README.md`
- Create: `docs/about/README.md`
- Create: `docs/.vuepress/components/HomePage.vue`

**Interfaces:**
- `HomePage.vue` renders the landing page using Vue template and router links.
- Markdown frontmatter supplies title, description, date, category, tags, and article ordering.

- [x] **Step 1: Write the homepage content shell** with title, description, navigation links, and latest article cards.
- [x] **Step 2: Write the Chinese sample article** with code block, metadata, and actionable reading sections.
- [x] **Step 3: Add category, tag, and about pages** with plain-language copy.
- [x] **Step 4: Add accessibility labels** for avatar, GitHub link, navigation, and cards.

### Task 3: Add original visual assets and styling

**Files:**
- Create: `docs/.vuepress/public/avatar.svg`
- Create: `docs/.vuepress/public/favicon.svg`
- Create: `docs/.vuepress/styles/index.scss`

**Interfaces:**
- Public asset URLs are `/blog/avatar.svg` and `/blog/favicon.svg` after deployment.
- SCSS defines shared tokens and classes consumed by `HomePage.vue`.

- [x] **Step 1: Draw the original SVG avatar** as a small silver-gray-haired guitarist bust with abstract, non-character-specific geometry.
- [x] **Step 2: Draw the favicon** from the mint/teal “KZ” monogram and small waveform motif.
- [x] **Step 3: Implement responsive light/dark styling** for the hero, cards, tags, links, grid background, focus states, and reduced motion.
- [ ] **Step 4: Check layout at desktop and narrow widths** through the generated static site.

### Task 4: Add GitHub Pages workflow and project docs

**Files:**
- Create: `.github/workflows/deploy.yml`
- Create: `.gitignore`
- Create: `README.md`

**Interfaces:**
- Workflow builds the site with pnpm and uploads `docs/.vuepress/dist` to GitHub Pages.
- README documents local setup, first push, repository Pages setting, and custom domain note.

- [x] **Step 1: Add deterministic GitHub Actions permissions and Node/pnpm setup.**
- [x] **Step 2: Add ignore rules** for dependencies, generated output, editor files, and local logs.
- [x] **Step 3: Document first-time setup and Pages source** as GitHub Actions.
- [x] **Step 4: Inspect workflow YAML and README commands** for consistency with package scripts.

### Task 5: Install and verify

**Files:**
- Modify: `pnpm-lock.yaml` (generated by pnpm)

- [x] **Step 1: Run `pnpm install`.** Completed after network access was granted; lockfile created.
- [x] **Step 2: Run `pnpm docs:build`.** Completed successfully; output is `docs/.vuepress/dist/`.
- [x] **Step 3: Check generated asset paths and navigation targets** under `/blog/` by static inspection.
- [x] **Step 4: Report exact verification results and any manual GitHub Pages action.**
